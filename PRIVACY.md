# Privacy

原境英语以本地优先为默认设计。以下说明对应当前 `0.2.1` 个人测试版。

## 保存在本机的数据

- 导入的 Markdown 文章及文章标题。
- 导入的 MP3／WAV 副本、Small.EN 转写结果和逐句时间范围。
- 生词本、查词记录、界面语言和播放相关状态。
- 已生成的原句录音、按需下载的词典真人录音及其来源信息。
- 用户配置的 AI 服务信息。

这些数据存放在 Electron 当前用户的数据目录中，不进入源码目录。当前 Windows 安装器在卸载时默认保留应用数据，便于后续重装恢复；需要彻底移除时应另行删除对应的当前用户数据目录。

## 完全在本机完成的功能

- 文章和音频资料管理。
- 本地英英词典、英汉参考和生词本。
- Small.EN 音频转写、主音频连续播放、句子跟随高亮与时间定位。
- 已缓存录音的播放。

Small.EN 转写不会上传完整音频或逐字稿。

## 会访问网络的功能

- 用户主动使用 AI 语境说明或自然原句朗读时，应用会把完成该次请求所需的选词、上下文或句子发送给用户配置的供应商。
- 用户主动获取尚未缓存的词典真人发音时，应用会访问 Wikimedia Commons，并保存返回的作者与许可证信息。
- 开发者显式运行 `npm run transcription:prepare` 时，会从清单锁定的官方来源下载 Small.EN 和 whisper.cpp Windows 运行库。

应用当前没有广告、行为分析或遥测系统。用户仍需自行阅读所选 AI 供应商的隐私政策、数据保留政策和计费规则。

## API Key

API Key 不应写入源码、`.env`、日志、截图或问题报告。应用在 Windows 可用时使用 Electron `safeStorage` 保存凭证；若安全存储不可用，应用不会保存新的 Key。供应商会在首次真实调用时验证 Key、模型名和账户状态。

## 分享问题报告

提交 GitHub Issue 前，请删除文章内容、音频、转写稿、API 地址中的私有信息以及任何凭证。不要上传正式应用数据目录。
