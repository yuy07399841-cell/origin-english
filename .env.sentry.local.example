# Sentry 配置模板（此文件进仓库，不含任何真实值）
#
# 使用方法：
#   1. 把本文件复制为 `.env.sentry.local`（同一目录）
#   2. 在 `.env.sentry.local` 里填入真实 DSN
#
# `.env.sentry.local` 已被 .gitignore 挡住（规则 `.env.*`），不会进入 Git 历史。
#
# DSN 在哪里找：
#   Sentry 左侧 Settings -> Projects -> origin-english -> Client Keys (DSN)
#   或建项目后的 Configure SDK 引导页
#
# DSN 是公开可写的上报标识，不是密钥；客户端本就需要内置它。
# 但仍然不要把 API Token / Auth Token 写进这个文件。

# 唯一必填项。形如 https://<public-key>@o<org-id>.ingest.us.sentry.io/<project-id>
SENTRY_DSN=

# 可选：上报环境标签，用于在 Sentry 里区分「本地开发」和「正式安装包」
# 留空则自动判断：打包后为 production，dev 运行为 development
SENTRY_ENVIRONMENT=

# 可选：覆盖 release 名。留空则用 package.json 的 version（dev 运行会自动加 -dev 后缀）
# 仅在你想手工演练「跨版本分组」时才需要设置
SENTRY_RELEASE=
